package main

//import (
//	"log"
//	"testing"
//)

var start int64 = 1452477600
var mac string = "18:0c:ac:ac:5f:85"

/*
func TestHourlySummary(t *testing.T) {
	db := InitDBClient(conf.Postgres)
	records := db.HourlySummary(mac, start)
	for i := 0; i < len(records); i++ {
		log.Println(records[i])
	}
}
*/

/*
func TestDailySummary(t *testing.T) {
	db := InitDBClient(conf.Postgres)
	records := db.DailySummary(mac, start)
	for i := 0; i < len(records); i++ {
		log.Println(records[i])
	}
}
*/
