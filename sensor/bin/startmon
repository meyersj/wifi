#!/bin/bash


if iwconfig | grep -q "mon0.*Mode:Monitor" ; then
    echo "running"
else
    if sudo airmon-ng start wlan0 | grep "monitor mode enabled on mon0" ; then
        echo "started"     
        exit 0
    else
        echo "not started"
        exit 1
    fi
fi
